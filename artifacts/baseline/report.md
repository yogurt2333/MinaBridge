# 原端最小运行验证

日期：2026-09-13。微信开发者工具 2.02.2608070；官方 wechatide CLI / skill 0.3.9。

项目：`samples/westore-cafe`。业务文件来自上游提交 `0fe5c7df8e90582dd0b89e283df7fe32e04413f9`。

通过真实元素点击及 page data 读取验证，没有用 setData 或直接调用业务方法替代点击。

| 步骤 | 结果 |
| --- | --- |
| 当前页面 | `packageWeStoreCoffee/pages/home/home` |
| 首页 | 35 款饮品，分类及价格数据加载成功 |
| 点击 `.md-tab[data-id="139"]` | activeCategoryId = 139 |
| 点击 `.md-item` | 规格页饮品为抹茶脑袋，基础价 17 元 |
| 点击大杯、珍珠 | 加价 3 + 2 元，totalPrice = 22 |
| 点击加入订单 | 结算页 1 件，22 元，规格为冰 / 标准 / 大杯 / 加料:珍珠 |
| 返回并再次加入 | 同一订单 2 件，44 元；原订单内容保留 |

证据：`sku.jpg`、`checkout.jpg`（单件状态）、`checkout-data.json`、`checkout-two-items.json`。

## 原样本已知问题和未验证范围

- 返回规格页后 `activeItemCount` 仍为 0，但再次加入确实追加到了原订单。活动订单提示存在刷新问题，未修改上游业务代码。不能把此问题归因于后续迁移。
- 图片源为空，截图中的占位区域符合原始数据。
- 没有直接数量加减按钮，当前验证的是重复加入。
- 没有验证完整重启后的持久化、所有类别/规格组合、搜索、删除、地址授权、支付或真机。
- 没有调用支付，未读取个人地址；运行结束时保留本地未支付的 2 件演示订单。

结论：分类 → 选规格 → 本地订单 → 结算的最小链路可运行，可作为 Vue 2 迁移第一轮基线；不代表整个上游项目所有功能通过。
