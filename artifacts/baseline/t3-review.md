# T3 跨页与存储验证

日期：2026-09-14；基线：2b3ef05；对应 Issue #4。

实现合成多页样本的首次参数、返回实例保留、生命周期、重新进入、同步存储和相对 CommonJS 数据模块。未将此结果等同于咖啡流程迁移完成。

## Standards

未发现阻塞性规范问题。AST 遍历与文本编辑存在少量重复，后续再次扩展时评估提取，不在本轮扩大重构。

## Spec

审查发现模块内部缺少 wx 适配导入、存储类型及隔离约定不明确。已复现模块失败并添加共享接口导入；存储命名空间在模块执行前初始化。拒绝 undefined 写入，序列化失败时保留旧值；类型与隔离语义已写入 README。

## 验证结果

`npm test` 11 项通过，`npm run typecheck` 通过。真实浏览器验证中文参数、返回保留计数和再次 onShow、重新进入恢复数字存储、删除、提示及视口信息；同来源下两个独立工程不串数据，返回原工程仍恢复数据。CLI 验证动态依赖、越界路径和循环依赖拒绝。

限制：浏览器原生历史同步、导航回调、完整微信 API 兼容尚未实现。视口安全区域和胶囊按零适配；源工程路径变化会改变存储命名空间。
