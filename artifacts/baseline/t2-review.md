# T2 列表交互审查

日期：2026-09-14。基线：d783465；对应 Issue #3。

## Standards

没有发现仓库硬性规范违规。发现内联 rpx 正则会改写引号字符串，与 WXSS 解析行为不同。已修复引号字符串和 URL 保护，并在真实浏览器断言 CSS 自定义属性字符串保持原值。

## Spec

1. 带 key 的 block 循环生成 Vue 2 不可编译模板。当前明确在迁移阶段拒绝此排列，报告文件及偏移位置；真实元素的循环 key 和嵌套别名已验证。
2. 文本插值未校验语法。已加入解析与源位置诊断，在写入输出前失败。

## 验证

新增回归先得到失败，再修复。`npm test` 全部 8 项通过；`npm run typecheck` 通过。浏览器真实操作验证列表选择、数字及对象 dataset、条件显示、输入更新、嵌套数组字段更新和滚动布局。测试不直接调用业务方法。

仅完成此切片覆盖的语法；尚未完成跨页、咖啡迁移、模型调用或自动 verify。
