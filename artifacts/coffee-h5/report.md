# 咖啡 H5 迁移验收

日期：2026-09-15。输入为仓库中的未修改 WeStoreCafe 样本，工具基线为 6f5e64d 加本轮 T4 改动。源摘要见 migration-report.json。

## 行为证据

真实 CLI 生成、Vite 构建、Chromium 390×844 视口点击，验证首页 35 款饮品，奶茶分类下进入抹茶脑袋，选择大杯与珍珠，金额 22 元；加入订单后结算 22 元，规格包含大杯和珍珠；返回再次加入后为同一个 orderId、2 件、44 元。浏览器未捕获 pageerror。

返回规格页仍显示“加入订单”，原端活动订单件数提示不刷新的行为保留并有断言；没有修改源业务代码来掩盖此问题。

地址和支付事件方法内的原始实现被替换为未接入提示，按钮同时显示未接入说明。支付点击不产生成功状态，生成页面不含原演示 finishPaid 或支付占位参数。此处理针对已识别的直接平台 API 引用，不宣称任意别名、模块间接调用都能识别。

## 视觉复核

已查看 sku.png、checkout.png：标题、规格、22/44 元和按钮可读可操作；补齐根容器高度后页脚位于底部。结算图包含验证支付按钮时产生的“支付未接入”提示。

差异：源数据图片地址为空，浏览器显示空图片区域及失败图标；未生成虚构商品图片。浏览器不复现微信状态栏、胶囊和 Skyline；字体、系统安全区、hover 时序及滚动条表现存在平台差异。当前使用 pointer 事件反馈 hover，静态单类 placeholder 样式映射至 ::placeholder。没有进行像素一致性评分。

## 审查与测试

Standards：无仓库硬性违规；记录 AST 遍历重复和可变分析结果参数两个非阻塞改进建议。修复占位样式丢失，以及 created/onLoad 时当前实例尚未进入页面栈的问题。

Spec：补充同一 orderId 断言、保留原端问题断言、持久化截图及视觉差异说明。

全量 12 项测试和类型检查通过。CLI 报告中的 verification 仍为 not-run：本次验证由开发测试执行，用户命令中的自动 verify、模型改写和修复闭环尚未实现。
