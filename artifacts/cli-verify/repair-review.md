# T7 有限修复

2026-09-16；基线 ec51955 加本轮实现。固定模型响应演示首次输出残留 rpx，验证失败，下一轮实际收到诊断并返回修复内容，重新构建和浏览器验收通过。此为确定性测试，不冒充真实模型完整咖啡实验。

覆盖默认两轮、零轮、持续失败、无效路径补丁、请求超时、浏览器缺失，以及上一轮代码失败后下一轮验证进程启动失败。保存每轮模型报告、Vue 文件快照和验证结果；不能改写源工程、测试或基线。

Standards：规范审查发现重复运行沿用旧验证报告、help 缺少修复参数，以及函数命名未准确表达职责。已重置每轮验证状态、补参数帮助并改为 rewriteAndVerifyWithRepair。

Spec：修复旧代码失败报告掩盖新环境故障的问题，回归先失败后通过。只有实际验证产生的代码/断言失败进入下一轮，环境或模型故障停止。没有增加自动恢复、无限重试或修改验收标准的能力。

验证：22 项全量测试通过，npm run typecheck 通过。
