# CLI 自动验证交付

日期：2026-09-16；基线：62c0e6a 加本轮 T6 修改。

正式执行 `node dist/cli.js migrate samples/westore-cafe --out output/verified-coffee-20260916 --verify`：构建、咖啡同一订单 22/44 元行为通过，截图待人工评审。运行结果见 coffee.json；其中截图路径相对于本次输出目录。

对上一轮真实 qwen3.5:9b 产物运行同一验证函数：构建通过，目标 CSS 检查发现残留 rpx，退出码 1，记录见 model-regression.json。此步骤没有再次调用模型，没有修复该产物。

19 项测试和类型检查通过。固定模型响应分别覆盖构建语法错误、残留 rpx、实际浏览器运行错误；浏览器安装缺失返回环境故障并保留构建通过。每轮使用新浏览器和独立构建目录；正常结束关闭浏览器与服务。

## 审查

Standards：无书面规范违规；记录咖啡场景与早期独立回归存在重复，后续抽取场景模块时保留外部验收断言。

Spec：已修复阶段信息只在最终清理后保存的问题，现于阶段切换和清理前保存；清理错误与常见系统错误分类为环境故障。超时在 Windows 调用 taskkill /T /F 清理工作进程树，其他系统结束独立进程组。本轮没有做真实 120 秒挂死的进程树清理实验，不将此项标为运行验证完成。

## 边界

验证总上限 120 秒，浏览器启动 30 秒、导航 15 秒、元素操作 10 秒。普通工程只验证入口渲染和 CSS 基础检查，不代表完整业务等价。咖啡使用工具内置受信任场景；不加载生成工程的 Vite 配置。测试和场景位于模型允许改写的页面文件之外。尚无执行沙箱、自动视觉评分、自动修复与断点恢复。
