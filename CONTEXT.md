# MinaBridge

MinaBridge 是本地 CLI，将已有原生微信小程序迁移为 Vue 2 H5。当前规格为 GitHub #1，任务及依赖见 docs/plans/v0.1-published.json。

术语：源工程是只读小程序输入；目标工程是 CLI 在独立空目录生成的普通 Vue 2 工程；基线是原端实测证据；验证是构建与浏览器真实交互；修复轮是根据验证失败生成受限变更后重跑验证。

测试边界已确认：真实 CLI → 目标工程构建 → 浏览器交互；模型接口可以替换为固定响应。不能用直接改页面数据或改验收断言假装通过。

首版咖啡基线：抹茶脑袋基础价17元，大杯3元、珍珠2元，单件22元、同订单两件44元。原端返回规格页的件数提示未刷新，属于已知问题。

2026-09-13 用户确定模型服务为本地 Ollama，模型 qwen3.5:9b。已通过本地 tags 接口确认模型存在；实际生成质量和资源使用尚待验证。不替换成其他模型。

用户授权依据技能、代码与测试自主推进常规工作，不对路由和常规工程决定反复确认。显著产品/架构取舍或无法可靠判断时再询问。联网代理只临时设置，不写入仓库配置。
